"""Twitter OAuth 2.0 PKCE — to be implemented in Phase 1.

Reuse the OAuth flow from twitter-digest (`oauth2_auth.py`). Keep tokens
in this app's DB, NOT in twitter-digest's data dir — fresh OAuth app per project.

Stub kept here so the import path settles before the Phase 1 work lands.
"""
