# packages/shared

Reserved for types that need to be shared between `apps/web` (TypeScript) and `apps/api` (Python).

Empty in Phase 0. Phase 2 may introduce a JSON schema or codegen here once the public API surface is locked.
